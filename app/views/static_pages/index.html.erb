<div class="hero">
	<br />
    <div class="frosty col-xs-10 col-xs-offset-1 text-center">
    <br />
    <h1>Learn Astronomy from the Pros</h1>
	<br />
    <h2>I'm an illusionist, Michael.</h2>
   	<br />
    <%= link_to 'Learn More', courses_path, :class => 'btn btn-success' %>
	<br /><br />
  </div>
</div>

<br />
<div class="col-xs-10 col-xs-offset-1 features text-center">

  <% @courses.limit(3).each do |course| %>
    <div class="col-sm-4">
      <h3><%= link_to course.title, course_path(course), :class => 'feature-course' %></h3>
      <br />
      <% if course.image.present? %>
        <%= image_tag course.image, :class => 'img-responsive img-thumbnail' %>
      <% end %>
    </div>
  <% end %>    

</div>